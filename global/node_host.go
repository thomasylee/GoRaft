package global

/*
type NodeHost struct {
	Url string
	ApiPort string
	RpcPort string
}

var NodeHosts []NodeHost

func LoadNodeHosts() {
	for _, host := range Config["node_hosts"].([]map[interface{}]interface) {
		newHost = NodeHost{
			Url: host["url"].(string),
			ApiPort: host["api_port"].(string),
			RpcPort: host["rpc_port"].(string),
		}
		NodeHosts = append(NodeHosts, newHost)
	}

	Log.Debug("Hosts:", len(NodeHosts))
}
*/
